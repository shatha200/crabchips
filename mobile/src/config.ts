// This app is white-labeled per restaurant client (same model as the Lovable
// site generator): one Expo build = one restaurant. Swap this ID (and the
// EAS/app.config branding) per client. Matches the seed data's fixed UUID.
export const RESTAURANT_ID = "00000000-0000-0000-0000-000000000001";
